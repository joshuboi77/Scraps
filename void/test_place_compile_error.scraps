# Compile-time errors for place() element type

print "=== Compile-time place() errors ==="

ib = int_box()
place(0:"a") -> ib   # Expect compile error: cannot place Str into BoxInt

sb = str_box()
place(0:1) -> sb      # Expect compile error: cannot place Int into BoxStr

