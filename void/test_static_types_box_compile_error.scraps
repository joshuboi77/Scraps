# Compile-time box element type errors

print "=== Compile-time box element type errors ==="

sb = str_box()
pack(1) -> sb  # Expect compile-time TYPE error: BoxStr with Int element

ib = int_box()
pack("x") -> ib  # Expect compile-time TYPE error: BoxInt with Str element

